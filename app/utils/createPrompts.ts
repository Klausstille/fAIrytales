export default function createPrompts({
    name,
    system,
    user,
}: {
    name: string;
    system: string;
    user: string;
}) {
    return {
        id: `wLlqVdoGi4B6fFh7FnEq`,
        name: name,
        system: system,
        user: user,
    };
}
