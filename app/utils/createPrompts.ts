export default function createPrompts({
    name,
    system,
    user,
}: {
    name: string;
    system: string;
    user: string;
}) {
    return {
        id: `O6271wmlHmDN1ykSlEeI`,
        name: name,
        system: system,
        user: user,
    };
}
